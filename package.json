{
  "name": "node-addon-patterns",
  "description": "Patterns for node.js addons that I've found useful and want to develop",
  "version": "0.0.1",
  "scripts": {
    "configure": "node-gyp configure && npm run build",
    "build": "node-gyp build && npm test",
    "test": "node example.js",
    "standard": "standard"
  },
  "devDependencies": {
    "eslint": "^1.10.3",
    "eslint-config-standard": "^4.4.0",
    "eslint-plugin-standard": "^1.3.1",
    "node-gyp": "^3.2.0",
    "standard": "^5.4.1"
  },
  "dependencies": {
    "nan": "^2.1.0"
  }
}
